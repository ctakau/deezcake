# Supabase — get these from your project's Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
# Server-only key for the accounting API (Settings > API > service_role).
# NEVER prefix this with NEXT_PUBLIC — it must stay server-side.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Owner email — only this account can access admin, payments, journal
NEXT_PUBLIC_OWNER_EMAIL=your-email@example.com
OWNER_EMAIL=your-email@example.com
